# Copilot Instructions for multi-agent-shogun

This file is automatically overwritten at runtime by `generate_copilot_instructions()`.
The source of truth is `instructions/{role}.md`.

If you see this content, the system has not yet been started.
Run `./shutsujin_departure.sh --copilot` to generate agent-specific instructions.
